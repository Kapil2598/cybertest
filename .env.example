# Example env file for local development
# Copy to `.env` and update values before running locally

# MongoDB connection URL
MONGO_URL=mongodb://127.0.0.1:27017/express_mongoose_stream_app

# App port
PORT=3000

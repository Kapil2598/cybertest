{
  "name": "express-mongoose-stream-app",
  "version": "1.0.0",
  "description": "Production-style Express.js app with Mongoose, streaming endpoint, and GitHub Actions CI/CD.",
  "main": "app.js",
  "scripts": {
    "test": "node --check app.js && node --check routes/stream.js",
    "start": "node app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.4",
    "mongoose": "^7.6.0"
  }
}
